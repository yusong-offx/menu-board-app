import 'package:flutter/material.dart';

class QRcode extends StatelessWidget {
  const QRcode({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("QRcode");
  }
}
